<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.Projet4" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="toolbarStyle">@style/MyToolbar</item>
        <item name="android:statusBarColor">@color/dark_blue</item>
    </style>

    <style name="Theme.Projet4" parent="Base.Theme.Projet4" />

    <style name="RoundedShapeAppearance.FloatingActionButton" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">50%</item>
    </style>

    <style name="MyFloatingActionButtonStyle" parent="Widget.MaterialComponents.FloatingActionButton">
        <item name="backgroundTint">@color/red</item>
        <item name="android:layout_margin">10dp</item>
        <item name="srcCompat">@drawable/ic_add_24</item>
        <item name="tint">@color/white</item>
        <item name="fabSize">normal</item>
        <!--        <item name="fabCustomSize">96dp</item>-->
    </style>


    <style name="MyToolbar" parent="Widget.AppCompat.Toolbar">
        <item name="android:background">@color/blue</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:gravity">center</item>
        <item name="colorControlNormal">@color/white</item>
    </style>

    <style name="ToolbarTitleTextAppearance">
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">20sp</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="CustomMenuStyle">
        <item name="android:background">@color/dark_blue</item>
    </style>

    <style name="myPopupTheme" parent="@style/ThemeOverlay.AppCompat.Dark.ActionBar">
        <!-- SUBMENU -->
        <item name="android:textColor">@color/white</item>
        <item name="android:tint">@color/white</item>
        <item name="android:backgroundTint">@color/blue</item>
    </style>

</resources>